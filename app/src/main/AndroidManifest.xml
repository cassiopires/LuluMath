<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fakedomain.admin.lulumath">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_lulu"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Soma" />
        <activity android:name=".Subtracao" />
        <activity android:name=".Multiplicacao" />
        <activity android:name=".Divisao"></activity>
    </application>

</manifest>